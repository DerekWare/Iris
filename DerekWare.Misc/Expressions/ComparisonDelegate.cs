namespace DerekWare.Query
{
    /// <summary>
    ///     Used to compare two objects for equality, converting types when necessary.
    /// </summary>
    public delegate bool ComparisonDelegate(object x, object y);
}
